@model IEnumerable<Vaja1.Models.Student>

@{
    ViewBag.Title = "Študenti";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Ustvari novega", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Ime študenta
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Age)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StudentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Age)
        </td>
        <td>
            @Html.ActionLink("Uredi", "Edit", new { id=item.StudentId }) |
            @Html.ActionLink("Podrobnosti", "Details", new { id=item.StudentId }) |
            @Html.ActionLink("Briši", "Delete", new { id=item.StudentId })
        </td>
    </tr>
}

</table>
